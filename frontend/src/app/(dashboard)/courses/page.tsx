import { PagePlaceholder } from "@/shared/components/layout/page-placeholder";

export default function CoursesPage() {
  return (
    <PagePlaceholder
      title="Courses"
      description="Browse and manage the courses, subjects, and teacher/student assignments."
    />
  );
}