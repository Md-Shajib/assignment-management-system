import { PagePlaceholder } from "@/shared/components/layout/page-placeholder";

export default function TeachersPage() {
  return (
    <PagePlaceholder
      title="Teachers"
      description="View teacher profiles and the courses each teacher is assigned to."
    />
  );
}