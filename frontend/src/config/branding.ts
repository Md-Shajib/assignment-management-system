/**
 * Artwork shown beside the sign-in form.
 *
 * The current file is a placeholder: drop the final asset into `public/images/`
 * and update `src` (and the intrinsic size) here — nothing else needs to change.
 */
export const LOGIN_ILLUSTRATION = {
  src: "/images/login-illustration.svg",
  width: 400,
  height: 300,
  alt: "Students attending an online class",
} as const;

/** Copy for the sign-in showcase panel. */
export const LOGIN_SHOWCASE = {
  title: "Distance Learning Programs",
  description: "Attend live and recorded classes at your own convenience.",
} as const;
